{% extends 'base.html.twig' %}

{% block title %}Goodies
{% endblock %}

{% block body %}
	<h1>Goodies</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ goody.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ goody.nom }}</td>
			</tr>
			<tr>
				<th>Quantite</th>
				<td>{{ goody.quantite }}</td>
			</tr>
			<tr>
				<th>Image</th>
				<td><img width="200" src="{{asset('uploads/imgesGoodies/'~goody.image)}}" alt=""></td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_goodies_index') }}">back to list</a>

	<a href="{{ path('app_goodies_edit', {'id': goody.id}) }}">edit</a>

	{{ include('goodies/_delete_form.html.twig') }}
{% endblock %}
